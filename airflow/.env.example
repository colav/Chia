# Airflow Environment Configuration Example
# Copy this file to .env.dev or .env.prod and adjust values

# Project Directory (Absolute path to the root of the project)
AIRFLOW_PROJ_DIR=.

# Environment (dev or prod)
ENVIRONMENT=prod

# Image tag to use from Docker Hub (e.g., latest, 3.1.0)
AIRFLOW_IMAGE_TAG=3.1.0

# External port for the API/Webserver (e.g., 8080 for Prod, 8081 for Dev)
AIRFLOW_API_PORT=8080

# External port for Flower (Celery monitoring)
AIRFLOW_FLOWER_PORT=5555

# Base path for persistent storage on the host
IMPACTU_AIRFLOW_PATH=/storage/airflow/prod

# Airflow User ID (usually 1000 on Linux)
AIRFLOW_UID=1000

# Default Admin User
_AIRFLOW_WWW_USER_USERNAME=admin
_AIRFLOW_WWW_USER_PASSWORD=colav

# GitHub Token for API access
GITHUB_TOKEN=

# Additional requirements if needed (space separated)
_PIP_ADDITIONAL_REQUIREMENTS=
